  <h2>Grocery Items</h2>
  <% if current_user %>
    <div class="well"><p>Add an item to your item by typing below:</p>
      <div class="span8">
        <%= form_for :item do |item| %>
          <%= item.label :name, :class => 'span8 input-large search-query' %>
          <%= item.text_field :name, :class => 'span8 input-large'  %><br />
          <%= select(:category, :id, @name_array) %>
          <br />
          <%= item.submit "Add", :class => 'btn btn-primary btn-sm' %>
        <% end %><br />
       
      </div>
    </div>
  </div><!-- Ends the layout's main div to create whitespace below -->
  <div class="jumbotron">
    <% @current = current_user %>
    <table class="table table-striped table-bordered table-condensed">
    <tr>
      <th>Produce</th>
      <th>Meat</th>
      <th>Frozen</th>
      <th>Deli</th>
    </tr>
    <tr>
      <td>
        <% @items.each do |item| %>
          <% if item.category == "Produce" && item.user_id == @current.id %>      
            <%= item.name %> <%= link_to '[x]', item, :method => :delete %><br />
          <% end %>
        <% end %>
      </td>
      <td>
        <% @items.each do |item| %>
          <% if item.category == "Meat" && item.user_id == @current.id %>      
            <%= item.name %> <%= link_to '[x]', item, :method => :delete %><br />
          <% end %>
        <% end %>
      </td>
      <td>
        <% @items.each do |item| %>
          <% if item.category == "Frozen" && item.user_id == @current.id %>      
            <%= item.name %> <%= link_to '[x]', item, :method => :delete %><br />
          <% end %>
        <% end %>
      </td>
      <td>
        <% @items.each do |item| %>
          <% if item.category == "Deli" && item.user_id == @current.id %>      
            <%= item.name %> <%= link_to '[x]', item, :method => :delete %><br />
          <% end %>
        <% end %>
      </td>
    </tr>
    <tr>
      <th>Pet</th>
      <th>Cleaning</th>
      <th>Dairy</th>
      <th>Pantry</th>
    </tr>
    <tr>
      <td>
        <% @items.each do |item| %>
          <% if item.category == "Pet" && item.user_id == @current.id %>      
            <%= item.name %> <%= link_to '[x]', item, :method => :delete %><br />
          <% end %>
        <% end %>
      </td>
      <td>
        <% @items.each do |item| %>
          <% if item.category == "Cleaning" && item.user_id == @current.id %>      
            <%= item.name %> <%= link_to '[x]', item, :method => :delete %><br />
          <% end %>
        <% end %>
      </td>
      <td>
        <% @items.each do |item| %>
          <% if item.category == "Dairy" && item.user_id == @current.id %>      
            <%= item.name %> <%= link_to '[x]', item, :method => :delete %><br />
          <% end %>
        <% end %>
      </td>
      <td>
        <% @items.each do |item| %>
          <% if item.category == "Pantry" && item.user_id == @current.id %>      
            <%= item.name %> <%= link_to '[x]', item, :method => :delete %><br />
          <% end %>
        <% end %>
      </td>
    </tr>
    </table>
  <% else %>
    <h2> Please sign in to create a list </h2>
  <% end %>
<!-- No ending div because its closed in the layout -->