<!DOCTYPE html>
<html>
<head>
  <title>NkuProject - Grocery list</title>
  <%= stylesheet_link_tag    "application", media: "all" %>
  <%= javascript_include_tag "application" %>
  <%= csrf_meta_tags %>
</head>
<body>
  <nav class="navbar navbar-default container" role="navigation">
    <div class="collapse navbar-collapse">
      <ul class="nav navbar-nav ">
        <% if current_user %>
          <br />Welcome <%= current_user.name %>!
        <% else %>
          <br />Welcome Guest! Sign in to view your saved lists.
        <% end %>
      </ul>
      <div class="nav navbar-nav navbar-right">
        <div class="btn-group">
          <% if current_user %>
            <%= link_to "Sign out", 'logout', :class => "btn btn-default navbar-btn" %>
          <% else %>
            <%= link_to "Sign Up!", new_user_path, :class => "btn btn-default navbar-btn" %>
            <%= link_to "Sign In!", new_session_path, :class => "btn btn-default navbar-btn" %>
          <% end %>
        </div>
      </div>
    </div>
  </nav>
  <div class="container">
    <%= render partial: "layouts/info_stuff" %>
    
    <div class="jumbotron">
      <%= yield %>
    </div>
  </div>
</body>
</html>
